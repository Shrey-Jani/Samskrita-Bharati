// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    domains: ["https://janishre.dev.fast.sheridanc.on.ca/"], // Replace with your domain
    unoptimized: true,
  },
};
// next.config.js

module.exports = nextConfig;
